diff --git a/install-phase1 b/install-phase1
index 8728d3d..676aeb2 100755
--- a/install-phase1
+++ b/install-phase1
@@ -86,23 +86,11 @@ print_step "Formatting partitions..."
 mkfs.fat -F32 -n EFI "$EFI_PART"
 mkfs.btrfs -L arch -f "$ROOT_PART"
 
-# Create btrfs subvolumes
-print_step "Creating btrfs subvolumes..."
-mount "$ROOT_PART" /mnt
-btrfs subvolume create /mnt/@
-btrfs subvolume create /mnt/@home
-btrfs subvolume create /mnt/@snapshots
-btrfs subvolume create /mnt/@var_log
-umount /mnt
-
-# Mount with SSD-optimized options
-print_step "Mounting btrfs subvolumes..."
+# Mount root partition directly (no subvolumes)
+print_step "Mounting btrfs partition..."
 BTRFS_OPTS="noatime,compress=zstd:1,space_cache=v2,discard=async"
-mount -o "${BTRFS_OPTS},subvol=@" "$ROOT_PART" /mnt
-mkdir -p /mnt/{home,.snapshots,var/log,boot/efi}
-mount -o "${BTRFS_OPTS},subvol=@home"      "$ROOT_PART" /mnt/home
-mount -o "${BTRFS_OPTS},subvol=@snapshots" "$ROOT_PART" /mnt/.snapshots
-mount -o "${BTRFS_OPTS},subvol=@var_log"   "$ROOT_PART" /mnt/var/log
+mount -o "$BTRFS_OPTS" "$ROOT_PART" /mnt
+mkdir -p /mnt/boot/efi
 mount "$EFI_PART" /mnt/boot/efi
 
 print_step "Partition layout:"
@@ -121,7 +109,10 @@ fi
 print_step "Installing base system..."
 pacstrap /mnt base
 if [ -f "$RACA_FONTS" ]; then
-	pacman -U -r /tmp $RACA_FONTS
+	FONT_FILENAME=$(basename "$RACA_FONTS")
+	cp "$RACA_FONTS" /mnt/root/
+	arch-chroot /mnt pacman -U --noconfirm "/root/$FONT_FILENAME"
+	rm /mnt/root/$FONT_FILENAME
 fi
 pacstrap /mnt $PACKAGES
 
@@ -182,7 +173,7 @@ refind-install
 print_step "Installing rEFInd theme..."
 REFIND_DIR="/boot/efi/EFI/refind"
 THEME_TMP=$(mktemp -d)
-git clone --depth 1 https://github.com/bobafetthotmail/refind-theme-regular "$THEME_TMP"
+git clone --depth 1 https://github.com/bobafetthotmail/refind-theme-regular "$THEME_TMP/refind-theme-regular"
 mkdir -p "$REFIND_DIR/themes"
 cp -r "$THEME_TMP/refind-theme-regular" "$REFIND_DIR/themes/"
 sed -i '/include themes\/refind-theme-regular/d' "$REFIND_DIR/refind.conf"
@@ -216,9 +207,6 @@ systemctl enable rtkit-daemon
 systemctl enable fstrim.timer
 systemctl enable seatd
 
-# Symlink resolv.conf for systemd-resolved
-ln -sf /run/systemd/resolve/stub-resolv.conf /etc/resolv.conf
-
 # journald size limit
 mkdir -p /etc/systemd/journald.conf.d
 cat > /etc/systemd/journald.conf.d/size.conf << EOF
diff --git a/pkglist b/pkglist
index 16bc7a7..a7d396b 100644
--- a/pkglist
+++ b/pkglist
@@ -30,6 +30,7 @@ rtkit
 seahorse
 seatd
 vivaldi
+vivaldi-ffmpeg-codecs
 wayland-protocols
 wl-clipboard
 udiskie
